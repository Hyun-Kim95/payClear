# Cursor — local session state
.cursor/state/
.cursor/quality-gate.json
.cursor/guard-shell.local.json

# Secrets and environment
.env
.env.*
!.env.example

# OS
.DS_Store
Thumbs.db
Desktop.ini

# Editor
.idea/
.vscode/
*.swp
*~

# Dependencies / build (app)
node_modules/
dist/
dev-dist/
build/
.next/
out/

# Logs and coverage
*.log
coverage/

# Local config (machine-specific)
.obsidian-ingest.json
_config/obsidian-repos.json

# Release binaries (do not commit)
releases/*.apk
releases/*.aab

# LLM wiki raw dumps (may contain sensitive originals; only distilled notes are committed)
docs/wiki/_raw/
